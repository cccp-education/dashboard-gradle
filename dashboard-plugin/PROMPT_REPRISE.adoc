= PROMPT_REPRISE — dashboard-gradle
:session: 216
:date: 2026-09-07

== Session accomplie (S-215, cross-borough MEMPHIS)

*1re release Maven Central + adoption version catalog ws (ROLLOUT-6)* :
1. Fix creds nmcp settings (pattern graphify — chargement explicite
   `~/.gradle/gradle.properties`)
2. dashboard-plugin 0.0.1 publié Central (bundle `e0619a49`, repo1 200)
3. Adoption pin ws : settings `from("education.cccp:workspace-catalog:0.0.34")`
   + `version = ws.versions.dashboard.plugin.get()`
4. Gate : check VERT 58 tests 0 FAIL (baseline exacte)

== Statut

- DSH-0→DSH-7, DSH-10 : ✅ TERMINÉS
- PUB (Maven Central) : ✅ TERMINÉ (S-215 — 0.0.1 sur Central)
- DSH-8 (GitHub Sync) : 0/8 pts PLANIFIÉ (optionnel)

== Règle à retenir (piège #13 MEMPHIS)

Le block `nmcpSettings { centralPortal { ... } }` n'hérite PAS des
credentials globaux — chargement explicite `~/.gradle/gradle.properties`
requis (pattern graphify/dashboard).

== Prochaine session (216) — à la demande

*Priorité P1* : DSH-8 Git Sync — rafraîchissement des INDEX.adoc /
BACKLOG.adoc des 30 boroughs par clone/pull git.

*Alternative P2* : maintenance (audit adoption catalog MEMPHIS, dette
bakery BKY-TEST-FLAKY).

== Vérifications ouverture

. `git status` — dashboard-gradle committé/poussé ?
. Central `<latest>` dashboard-plugin 0.0.1
. `./gradlew check` VERT (58/0, version dérivée ws — nécessite mavenLocal
   ou Central catalog 0.0.34)