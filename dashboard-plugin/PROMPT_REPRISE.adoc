= PROMPT_REPRISE — dashboard-gradle
:session: 010
:date: 2026-06-23

== Mission accomplie (S009)

*DSH-7 Intégration runner-gradle* :
1. Domaine `dashboard.publish` — `PublishDashboardTask` (`@InputDirectory`, `@OutputDirectory`, `@CacheableTask`)
2. `DashboardConstants` factorise le groupe de tâches `dashboard`
3. `DashboardPlugin` expose `publishDashboard` comme tâche typée consommable par un orchestrateur N3
4. Tests : 31 unit + 6 functional + 10 Cucumber = 47/47 JUnit PASS + 10/10 Cucumber PASS

== Statut

- DSH-0 (Bootstrap gouvernance) : 3/3 pts ✅
- DSH-1 (Plugin scaffold) : 8/8 pts ✅
- DSH-2 (IndexCrawler) : 13/13 pts ✅
- DSH-3 (Dashboard theme + vues) : 13/13 pts ✅
- DSH-4 (EPIC Matrix par borough) : 8/8 pts ✅
- DSH-5 (DAG visualization) : 8/8 pts ✅
- DSH-6 (Timeline MVP0) : 5/5 pts ✅
- DSH-7 (Intégration runner-gradle) : 5/5 pts ✅
- DSH-8 (GitHub Sync) : 0/8 pts PLANIFIE
- PUB (Maven Central) : 0/5 pts PLANIFIE

== Prochaine session (010)

*Priorité P1* : PUB — Publication Maven Central nmcp.

1. Vérifier la configuration nmcp (settings + build.gradle.kts)
2. S'assurer que toutes les dépendances sont des releases (pas de SNAPSHOT)
3. `./gradlew build -x test` puis `./gradlew publishAggregationToCentralPortal --no-daemon` (sur demande explicite)
4. Mettre à jour la documentation et le portefeuille

*Alternative P2* : DSH-8 Git Sync — rafraîchissement des INDEX.adoc / BACKLOG.adoc par clone/pull git des boroughs (pas d'API GitHub spécifique).
