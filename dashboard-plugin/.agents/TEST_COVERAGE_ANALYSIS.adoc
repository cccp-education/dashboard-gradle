= Analyse de Couverture — dashboard-gradle

== Metriques

|===
| Metrique | Valeur
| Tests unitaires (JUnit5 + AssertJ) | 31
| Tests fonctionnels (Gradle TestKit) | 6
| Tests Cucumber BDD | 10 scenarios
| Total | 57 PASS
|===

== Details des tests

=== Unitaires — IndexCrawlerTest (7)

. `should parse INDEX_adoc with borough table and epic table`
. `should return empty data for file without tables`
. `should parse borough with VESTIGE status based on role`
. `should crawl all INDEX_adoc files in a directory`
. `should skip non-INDEX_adoc files`
. `should parse SESSIONS_HISTORY_adoc into session activities`
. `should crawl SESSIONS_HISTORY_adoc alongside INDEX_adoc`

=== Unitaires — DashboardPluginTest (4)

. `plugin should register extension and tasks`
. `tasks should belong to dashboard group`
. `extension should have default values`
. `extension should accept custom values`

=== Unitaires — PublishDashboardTaskTest (4) — S009

. `task should belong to dashboard group and have description`
. `task should expose outputDir as input and publishDir as output`
. `task should copy generated dashboard output to publish directory`
. `task should skip copy when output directory does not exist`

=== Unitaires — DashboardRendererTest (6) — S008

. `render should produce index html with epic matrix`
. `render should include activity stream section when sessions present`
. `render should copy dashboard css to output directory`
. `epic status should map to css class`
. `render should group epics by borough in html table`
. `render should include timeline section with milestone entries`

=== Unitaires — BoroughGroupTest (4) — S006

. `should group epics by borough sorted by dag level`
. `should sort epics inside borough by priority then id`
. `should expose borough status as string`
. `should create UNASSIGNED group for epics without borough`

=== Unitaires — DagGraphBuilderTest (5) — S007

. `build should create nodes from borough data`
. `build should create edges between consecutive DAG levels`
. `build should create multiple edges when several nodes depend on all previous level nodes`
. `build should fallback to dagNodes when boroughs are empty`
. `build should handle empty data`

=== Unitaires — Timeline domaine (11) — S008

==== MilestoneTest (3)

. `milestone should expose its properties`
. `milestones should be ordered by date then by id`
. `milestones with same date should be ordered by id`

==== TimelineTest (3)

. `timeline should keep milestones sorted by date`
. `milestonesUpTo should return only milestones before or on the given date`
. `byType should filter milestones by type`

==== TimelineBuilderTest (5)

. `build should create a milestone for each done epic dated by its session`
. `build should use planned future date when no session matches`
. `build should map in-progress epic to milestone status in progress`
. `build should skip epics without borough`
. `build should include borough dag level in milestone`

=== Fonctionnels — CrawlDashboardFunctionalTest (2)

. `crawlDashboard should produce JSON output when foundry has INDEX_adoc files`
. `crawlDashboard should warn when config path does not exist`

=== Fonctionnels — GenerateDashboardFunctionalTest (2)

. `generateDashboard should produce index html with epic matrix`
. `generateDashboard should include activity stream from sessions history`

=== Fonctionnels — PublishDashboardFunctionalTest (2) — S006/S009

. `publishDashboardSite should copy generated dashboard JBake site to publish directory` (S010)
. `publishDashboardSite output directory should be consumable by another Gradle task` (S009/S010)

=== Cucumber BDD (10 scenarios)

. `Crawl INDEX.adoc files in a foundry directory`
. `Crawl with missing config directory`
. `Crawl INDEX.adoc with epic table`
. `Crawl SESSIONS_HISTORY.adoc for activity stream`
. `Generate dashboard site from crawled data`
. `Generate dashboard site with activity stream`
. `Generate dashboard site with epic matrix grouped by borough` (S006)
. `Generate dashboard site with timeline` (S008)
. `Publish dashboard site copies generated output to publish directory` (S006)
. `Publish dashboard output is consumable by runner-gradle` (S009)

== Notes

*Session 000* : Projet bootstrappe — zero code source, zero test.
*Session 001* : 4 tests PASS (DashboardPluginTest).
*Session 002* : 14 tests PASS (9 unit + 2 functional + 3 Cucumber).
*Session 003* : Refactoring — pas de nouveau test.
*Session 004* : 13 tests PASS (+2 tests unitaires SessionActivity).
*Session 005* : 23 tests PASS — pipeline DDD/TDD/BDD complète pour le rendu statique.
*Session 006* : 30 tests PASS — EPIC Matrix groupée par borough + tâche publishDashboard + dogfood root.
*Session 007* : 31 tests PASS — Renommage packages, DSH-5 DAG visualization, traduction anglaise.
*Session 008* : 42 tests PASS + 9 Cucumber scenarios — DSH-6 Timeline MVP0 (domaine `dashboard.timeline`, intégration renderer/template, parsing session).
*Session 010* : 47 tests PASS + 10 Cucumber scenarios — site JBake-compatible consommable par bakery-gradle `serve` (`jbake/`, `publishDashboardSite`, template `[(${content.body})]`, CSS path `css/styles.css`, serve OK localhost:8820).
